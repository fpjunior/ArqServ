# Backend Configuration
NODE_ENV=production
PORT=3005

# Database Configuration (Neon)
DB_HOST=ep-xxx.neon.tech
DB_PORT=5432
DB_NAME=arqserv_db
DB_USER=arqserv_user
DB_PASSWORD=sua_senha_aqui

# JWT Configuration
JWT_SECRET=seu_jwt_secret_super_seguro_aqui

# CORS Configuration
CORS_ORIGIN=https://seu-frontend.vercel.app

# Google Drive OAuth 2.0 Configuration (Obrigatório para uploads)
# MÉTODO 1: OAuth 2.0 (RECOMENDADO - Uploads ilimitados)
# 1. Acesse https://console.cloud.google.com/
# 2. Crie/selecione um projeto e ative a Google Drive API
# 3. Vá em "Credenciais" > "Criar credenciais" > "ID do cliente OAuth 2.0"
# 4. Configure a tela de consentimento OAuth
# 5. Adicione seu email como usuário de teste
# 6. Baixe as credenciais OAuth e use os valores abaixo:
GOOGLE_DRIVE_CLIENT_ID=seu_client_id_oauth.apps.googleusercontent.com
GOOGLE_DRIVE_CLIENT_SECRET=seu_client_secret_oauth
GOOGLE_DRIVE_REFRESH_TOKEN=seu_refresh_token_oauth

# MÉTODO 2: Service Account (FALLBACK - Limitado por quota)
# Se preferir Service Account, mantenha o arquivo credentials na pasta backend/credentials/
GOOGLE_DRIVE_ROOT_FOLDER_ID=sua_pasta_id_aqui
